[pytest]
# The pytest plugin that ships with the mockagents SDK serves the agent
# definitions in ../agents — one agent, shared by every recipe here.
mockagents_agents_dir = ../agents
asyncio_mode = strict
