# OpenAI Agents SDK recipe. Install in its OWN virtualenv: this pulls
# openai>=3, which conflicts with CrewAI's openai<3 pin.
openai-agents>=0.20
pytest>=8.0
pytest-asyncio>=0.24
# NOT ON PYPI YET. Until the release pipeline publishes it, install the SDK
# from this checkout instead:
#     pip install -e <repo>/sdk/python
# Uncomment once `pip install mockagents` resolves (see the install table
# in the repo README, which the install-paths workflow checks daily).
# mockagents>=0.4.0
