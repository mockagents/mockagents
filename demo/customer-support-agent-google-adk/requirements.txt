# Google ADK (the agent framework) + LiteLLM (for the alternative bridge backend).
# google-genai (the Gemini client ADK uses) and httpx come in transitively, but
# we pin them since the framework-free scripts import them directly.
google-adk>=1.0.0
litellm>=1.50.0
google-genai>=1.0.0
httpx>=0.27.0
