# The application's own dependencies.
openai>=1.40
# Both MCP SDK majors work: the 1.x -> 2.0 bump renamed CallToolResult.isError
# to .is_error, and app/retrieval.py reads either.
mcp>=1.9

# The test harness: the pytest plugin ships inside the mockagents SDK, so no
# conftest.py and no imports are needed to get a running mock server.
#
# NOT ON PYPI YET, so this line stays commented -- leaving it active makes
# `pip install -r requirements.txt` fail outright, before it installs anything.
# Install the SDK from this checkout instead:
#     pip install -e ../../sdk/python
# Uncomment once `pip install mockagents` resolves.
# mockagents>=0.4.0
pytest>=8.0
pytest-asyncio>=0.24
