# Action Plan — `internal/server`

_Execute top-down. Each task maps to a finding ID (detail in `01-PER-FILE.md` / `02-INTEGRATION.md`). Check off when its "Done when" holds. Self-contained — you don't need the other reports to act._

**Gate:** the **P0** box must be checked before the next **multi-tenant** release. (Single-tenant / LLM-mock usage is unaffected by P0.)

## P0 — Blockers (gate the multi-tenant release)

- [x] **Enforce tenant ownership on the management API (cross-tenant IDOR)** — `X-SEC-001` / `F-TN-001..005` · effort:M · owner:claude · **DONE 2026-06-02**
  - **Fix shipped (store-level scoping + handler ownership gate):** `tenancy/store.go` `UpdateAPIKeyRole`/`RotateAPIKey`/`DeleteAPIKey` now take `tenantID` and add `AND tenant_id = ?` to their SELECT/UPDATE/DELETE — a cross-tenant key id affects 0 rows → `ErrNotFound` → 404, atomically (no TOCTOU). The tenant-addressed handlers (`ListAPIKeys`/`CreateAPIKey`/`BulkRotateTenantKeys`) gate on a new `ensureOwnTenant` helper that 404s when the path `{id}` ≠ `principal.TenantID`. The key-addressed handlers pass `principal.TenantID`; the self-service `/keys/me/*` handlers pass `(principal.TenantID, principal.KeyID)` (unchanged behavior, now explicitly scoped). Store-level scoping is defense-in-depth — no future handler can forget the check.
  - **Done when:** ✅ `TestTenancyHandlers_CrossTenantIDOR_Returns404` — a tenant-A admin's rotate/delete/demote/list/create/bulk-rotate of tenant-B resources all return 404 and B's key still resolves with its original role + count. **Regression-verified:** neutering the `ensureOwnTenant` guard + one store scope makes 5/6 cross-tenant ops succeed (200/201) and rotates B's key. Updated existing handler tests to inject an authenticated principal (`servePrincipal` helper). Full `go test ./...` green (19 pkgs); build/gofmt/vet clean. On branch `fix/sec-001-tenant-idor`.
  - **Note:** tenant-level CRUD (`CreateTenant`/`ListTenants`/`DeleteTenant`) remains role-gated but NOT super-admin-scoped — a per-tenant admin can still list/create/delete tenants. That's a separate "no super-admin tier" design gap (`X-SEC-002` territory), out of scope for this key-IDOR fix; flagged for a follow-up.

## P1 — High (this cycle)

- [x] **Cap request body sizes** — `X-DOS-001` / `F-VL-001` / `F-TN-007` · effort:S · owner:claude · **DONE 2026-06-02**
  - `validate_handler.go` wraps the body in `http.MaxBytesReader(w, r.Body, maxValidateBodyBytes=1 MiB)` and returns **413** on a `*http.MaxBytesError` (also moved `defer r.Body.Close()` before the read — `F-VL-002`). The tenancy handlers gained a shared `decodeJSONBody` helper (`maxJSONBodyBytes=64 KiB`) used by `CreateTenant`/`CreateAPIKey`/`UpdateAPIKeyRole` → 413 on oversize, 400 on malformed. **Done when:** ✅ `TestValidateHandler_OversizedBody` + `TestTenancyHandlers_OversizedBody` (both → 413). _(YAML alias-bomb depth bound in `config.ValidateBytes` still a separate dependency check — see "Needs investigation".)_ On branch `fix/server-p1-hardening`.
- [x] **Role-gate `ReloadAgent`** — `F-HD-001` + `F-HD-002` · effort:S · owner:claude · **DONE 2026-06-02**
  - `F-HD-001`: the reload route is now mounted as `tenancy.RequireRole(tenancy.RoleEditor, …)` in multi-tenant mode (single-tenant stays open, like the audit route). `F-HD-002`: the reload loop matches the on-disk file by **name AND `Metadata.TenantID == existing.Metadata.TenantID`** (`tenant_id` is a real YAML field), so a same-name file from another tenant can't be registered over the caller's agent. **Done when:** ✅ `TestReloadAgent_GatedInMultiTenant` (anonymous → 401, admin → 200); the F-HD-002 tenant match is exercised by the gate test's happy path and verified by inspection. On branch `fix/server-p1-hardening`.
- [x] **Fix the async-log-worker send-on-closed-channel race** — `F-LW-001` + `F-LW-004` · effort:S · owner:claude · **DONE 2026-06-02**
  - Added `mu sync.RWMutex` to `LogWorker`: `Submit` holds `RLock` around the stopped-check + (non-blocking) queue send; `Shutdown` takes the write `Lock` before `stopped.Store(true); close(queue)`. No `Submit` send can now straddle the close. **Done when:** ✅ `TestLogWorker_SubmitDuringShutdown` (50 iters × 16 goroutines × 200 submits during a concurrent Shutdown) never panics — **regression-verified**: neutering the locks reproduces `panic: send on closed channel`. On branch `fix/server-p1-hardening`.

## P2 — Medium (schedule)

- [x] **Unify route authorization policy** — `X-AUTHZ-001` / `F-CO-005` / `F-PL-001` · effort:S — define the role floor for every `/api/v1/*` route in one place; decide costs/pipelines (likely `viewer`) and reload (`editor`). **Done when:** a single table/test asserts the floor per route. **DONE 2026-06-02** — new `route_authz.go`: `managementRouteFloors` map is the single source of truth (route→`Role`, `roleOpen=""` for no-floor), and `(*Server).mountManaged` is the one chokepoint that applies `tenancy.RequireRole(floor, …)` in multi-tenant mode (open in single-tenant). `registerRoutes` now mounts every `/api/v1` management route through it. Closed the flagged gaps: `costs`→viewer (F-CO-005), `pipelines`+`pipelines/{name}`→viewer (F-PL-001); reload stays editor (F-HD-001). An unknown pattern **panics** at registration → an ungated route can't slip in again (caught by the construction in `setupTenantServer` + the explicit test). Tests in `route_authz_test.go`: `TestMountManaged_AppliesFloorInMultiTenant` (viewer 403'd on admin route; viewer admitted to costs), `_OpenInSingleTenant`, `_PanicsOnUnknownRoute`, `TestManagementRouteFloors_FlaggedRoutes` (neuter-verified) + `_AllValid`. Full `go test ./...` green.
- [x] **Fix live-feed route mounting order** — `F-SRV-ORDER-001` · effort:S · **DONE 2026-06-02** — `New()` called `registerRoutes` (server.go:133) **before** `s.logBroadcaster`/`s.logWorker` were assigned (:145), so the `if s.logBroadcaster != nil` block was skipped and `GET /api/v1/logs/stream[/metrics]` never mounted (dead in production) and `LogHandlers.Broadcaster` was nil. Moved the broadcaster/worker construction **above** `registerRoutes` (the middleware chain still runs after, so `InteractionCapture` wiring is unchanged). Test `TestNewServer_MountsStreamRoutes` asserts `/logs/stream/metrics` → 200 (same `logBroadcaster != nil` guard as `/logs/stream`, but non-blocking); regression-verified — reverting the order yields 404. Full `go test ./...` green.
- [x] **Close SSE broadcaster before httpServer.Shutdown** — `F-SRV-SHUT-002` · effort:S · **DONE 2026-06-02** — now that the SSE routes actually serve (F-SRV-ORDER-001), `Server.Shutdown` called `httpServer.Shutdown(ctx)` (which *waits* for the in-flight `/logs/stream` handler) **before** `logBroadcaster.Close()` (which unblocks it via the closed `sub.C()`), so a client connected at shutdown pinned graceful shutdown for the full `ShutdownTimeout` (5s). Moved `logBroadcaster.Close()` to the **top** of `Shutdown`, before `httpServer.Shutdown`; the later log-worker drain's Publishes and any Subscribe in the shutdown window are already safe no-ops on a closed broadcaster (F-SV-001). Test `TestServerShutdown_UnblocksLiveFeed` (subscribes via a real in-flight `/logs/stream` request detected through `SubscriberCount()`, asserts Shutdown returns < 2s; regression-verified — reverting the order blocks ~5s). Full `go test ./...` green.
  - **Note (test technique):** an end-to-end SSE client `Do()` over the full middleware chain didn't surface response headers in-test, so the shutdown test detects the in-flight handler via the broadcaster's `SubscriberCount()` rather than waiting on client headers. The middleware flush path itself looks correct (`statusWriter`/`statusRecorder` both delegate/promote `Flush`); the header-buffering was a test-client artifact, not confirmed as a prod bug — left unflagged.
- [x] **Shutdown ordering: stop workers before closing the store; close the broadcaster** — `X-SHUT-001` / `F-LW-002` / `F-SV-001` · effort:M · owner:claude · **DONE 2026-06-02**
  - `X-SHUT-001`/`F-LW-002`: `LogWorker` gained a `quit` channel + a cancellable `ctx`. Workers now `select` on `quit` (and on a closed/drained queue) and derive each write's context from `w.ctx`. `Shutdown` closes the queue, then at the drain deadline a `time.AfterFunc` cancels `w.ctx` (aborting any in-flight `store.Log`) and closes `quit` (stops ranging), and **always `wg.Wait()`s** — so no worker can touch the store after `Shutdown` returns. start.go already calls `srv.Shutdown()` before the deferred `logStore.Close()` (verified), so the use-after-close window is closed. `F-SV-001`: added `LogBroadcaster.Close()` (closes every subscriber channel, makes Subscribe-after-close return a closed sub, Publish a no-op; idempotent; cancel-after-close safe), called from `Server.Shutdown` after the worker drains. **Done when:** ✅ existing worker/drain/race tests green + `TestLogBroadcaster_Close` (channel closed, post-close subscribe closed, double-close + cancel-after-close safe). Full `go test ./...` green. On branch `fix/server-p2-lifecycle`.
- [x] **Single error envelope + no raw-error leak** — `X-ERR-001` / `F-TN-006` · effort:S · owner:claude · **DONE 2026-06-02** — added shared `writeError(w,status,msg)` (flat `{"error":msg}`, the handlers' existing shape — and the GUI reads error bodies as raw text, so no wire-compat break) and `writeServerError(w,err)` (logs the real error server-side via `slog`, returns a generic 500). Migrated the 8 tenancy 500-sites + the audit/costs store-error sites off raw `err.Error()`; `CreateTenant`/`CreateAPIKey`/`UpdateAPIKeyRole` post-validation store failures now go through `writeServerError`. **Done when:** ✅ `TestTenancyHandlers_StoreError_DoesNotLeak` (closed store → 500 body is `"internal error"`, no `sql`/`database`/`closed`/`sqlite` text). **Note:** the auth middleware's nested `{"error":{type,message}}` shape (tenancy pkg) is intentionally left for auth errors — fully merging the two shapes is a deferred wire decision (low impact: GUI reads raw text).
- [x] **Shared `limit` clamp** — `X-LIMIT-001` / `F-LH-002` / `F-AU-003` · effort:S · owner:claude · **DONE 2026-06-02** — added `parseBoundedInt(w, value, param, min, max)` + `const maxListLimit=10000`; logs (limit+offset), audit, and costs all route through it (costs' old hardcoded 10000 → the constant). Above-max values clamp to max instead of erroring; below-min / non-integer → 400. **Done when:** ✅ `TestParseBoundedInt` (clamp/reject/pass-through incl. `99999999`→max).
- [x] **SSE capture: skip body buffering + tolerant content-type match** — `F-LH-001` / `F-LH-004` / `X-SSE-001` · effort:S — set `cw.capture=false` once `text/event-stream` is seen; match via prefix/`mime.ParseMediaType`. **Done when:** an SSE response is not buffered into `cw.body` and is labeled streaming even with a charset param. **DONE 2026-06-02** — `captureWriter` gained `streaming`/`sniffed` fields and a one-shot `detectStreaming()` called from both `WriteHeader` and `Write`: when the final Content-Type is an SSE stream it sets `streaming=true` + `capture=false`, so chunks are never appended to `cw.body`. New `isEventStream()` uses `mime.ParseMediaType` (tolerates `; charset=utf-8`, case-insensitive) with a `strings.HasPrefix` fallback for malformed headers. The middleware now reads `cw.streaming` instead of re-parsing the header with exact equality. Tests: `TestInteractionCapture_SSENotBuffered` (charset param; asserts empty body + `Streaming=true` + full passthrough to client; neuter-verified — body is buffered when detection is removed) and `TestIsEventStream` (table). Full `go test ./...` green.
- [x] **Audit tenant scoping (or document global-admin-only)** — `X-SEC-002` / `F-AU-005` · effort:M — add a tenant marker + filter, or gate audit read as a global-admin capability. **Done when:** a tenant-A admin cannot read tenant-B audit events (or the global-admin contract is explicit + enforced). **DONE 2026-06-02** — events already carried an `actor_tenant` column; added an `ActorTenant` filter to `audit.Query` (+ `WHERE actor_tenant = ?` in `SQLiteStore.List`) and scoped `AuditHandlers.ListEvents` to the caller's own tenant via `tenancy.PrincipalFrom` (set `q.ActorTenant = p.TenantID` when a principal with a non-empty tenant is present). There is no super-admin tier, so this applies to every authenticated caller; single-tenant/local-dev mode (no principal) stays unfiltered. Tests: `audit.TestListFilterByActorTenant` (store), `server.TestAuditHandlers_TenantScoped` (cross-tenant leak — neuter-verified: unscoped admin sees all 4 events vs 2) + `TestAuditHandlers_SingleTenantUnfiltered`. Full `go test ./...` green.
- [x] **`audit_handlers` nil-store guard** — `F-AU-001` · effort:S · owner:claude · **DONE 2026-06-02** — `ListEvents` now returns 503 when `h.Store == nil`, mirroring costs/pipelines.
- [x] **Watcher lifecycle hardening** — `F-WT-001` / `F-WT-003` / `F-WT-004` / `F-WT-006` · effort:M — `Stop()` stops pending timers + drains in-flight `reloadFile` via a `closed` flag + `WaitGroup`; guard `Stop()` with `sync.Once`; normalize map keys with `filepath.Clean`. **Done when:** `TestWatcher_StopCancelsPendingReload` passes (write→Stop→no registry mutation after Stop) and double-`Stop` is race-safe. **DONE 2026-06-02** — `AgentDirWatcher` gained `closed`/`stopOnce`/`wg`. `Stop()` runs once under `stopOnce`: sets `closed` + stops & clears all pending timers under `mu`, cancels the loop, joins it, then `wg.Wait()`s in-flight reloads before closing the fsnotify handle. `schedule()` bails when `closed` and `filepath.Clean`s the path; the debounce callback re-checks `closed` under `mu` and registers with `wg` before releasing the lock (Add strictly precedes Stop setting `closed`, so no Add-after-Wait). `reloadFile()` also cleans its path so `fileAgents` keys stay canonical. Tests: `TestWatcher_StopCancelsPendingReload` (neuter-verified — fails when the timer-stop + closed-check are removed), `TestWatcher_ConcurrentStop` (8 goroutines, no deadlock), `TestWatcher_PathNormalization` (single canonical key). Full `go test ./...` green.
- [x] **`errors.Is` for sentinels** — `F-SV-002` / `F-SV-003` / `F-WT-005` · effort:S — replace `==`/substring error checks with `errors.Is` (incl. `isTransientMissing` → `errors.Is(err, fs.ErrNotExist)`). **Done when:** wrapped sentinels route to the correct status / branch. **DONE 2026-06-02** — `handleProcessRequest` now uses `isNotFound(err)` + `errors.Is(err, engine.ErrEmptyMessage)` (the old `err == engine.ErrAgentNotFound` was dead — the engine wraps it with `%w`, so `==` never matched) (F-SV-002); `isNotFound` dropped its `strings.Contains(err.Error(), …)` fallback — `errors.Is` is sufficient and the substring could misclassify unrelated errors as 404 (F-SV-003); `isTransientMissing` (watcher) now keys on `errors.Is(err, fs.ErrNotExist)` instead of locale-fragile `"no such file"`/`"cannot find the file"` substrings — verified `config.LoadFile`→`os.ReadFile`'s `*PathError` wraps with `%w` so `fs.ErrNotExist` matches cross-platform (F-WT-005); also swapped `Serve`'s `err != http.ErrServerClosed` → `!errors.Is(...)` (F-SV-009). Tests `TestIsNotFound` + `TestIsTransientMissing` (both neuter-verified: reverting to the substring form fails them); real-path `TestWatcherUnregistersDeletedFile` still green. Full `go test ./...` green.
- [x] **Status-code correctness in tenancy create paths** — `F-TN-008` / `F-TN-009` · effort:S — **DONE 2026-06-02** (completes the earlier partial). Added a `tenancy.ErrConflict` sentinel + `isUniqueViolation(err)` in the store (type-matches the modernc `*sqlite.Error` code `SQLITE_CONSTRAINT_UNIQUE`/`PRIMARYKEY`, not message text); `CreateTenant` now returns `%w`-wrapped `ErrConflict` on a duplicate name, and the handler maps `errors.Is(…ErrConflict)` → **409** (was a generic 500). Only `tenants.name` is UNIQUE, so that's the sole conflict path (`CreateAPIKey`/`UpdateAPIKeyRole` keep their 404/500 mapping from the partial). `F-TN-009`: `BulkRotateTenantKeys` now guards `len(oldPrefixes) == len(results)` before the lockstep audit loop → a contract violation is a clean 500 instead of an index-out-of-range panic + partial audit. Tests (new `tenancy_conflict_test.go`): `TestStore_CreateTenant_DuplicateIsConflict`, `TestTenancyHandlers_CreateTenant_DuplicateReturns409`, `TestTenancyHandlers_BulkRotate_LengthMismatchIs500` (fake mismatched-slice store) — all neuter-verified (revert → 500 / panic-EOF). Full `go test ./...` green.
- [ ] **`SetDenialHook` global-mutation footgun** — `F-SV-007` · effort:S — document single-server-per-process or guard the global. **Done when:** noted/guarded.
- [ ] **CORS + auth-header robustness** — `F-MW-001` / `F-MW-002` · effort:S — make CORS origins configurable; parse the `Bearer` scheme case-insensitively + trim. **Done when:** CORS origin is configurable and a malformed `Authorization` is handled deliberately.
- [ ] **Add the missing handler tests** — `X-TEST-001` / `F-SV-008` / `F-MW-007` / `F-CO-001` / `F-AU-002` / `F-TN-011` / `F-HD-005` / `F-LW-004` · effort:M — server-wiring RBAC tests (per-role 401/403/200), an SSE `StreamLogs` test, costs/audit handler tests, a secret-not-logged test, and the IDOR tests from P0. **Done when:** each listed gap has a test.

## P3 — Low (opportunistic)

- [ ] **Dead/odd code** — `F-LH-009` (remove `init(){_=json.Marshal}`), `F-PL-002` (dead `h==nil`), `F-TN-012` (misleading "secret-wipe" comment) · effort:S.
- [ ] **Doc drift** — `F-AU-004` (audit kind list omits `api_key.rotated`), `F-SV-006` (`Addr()` doc), `F-WT-008` (non-recursive watch), `F-CO-003` (costs `limit` truncates totals) · effort:S.
- [ ] **Input validation parity** — `F-CO-004` / `F-LH-011` (RFC3339-validate `since`/`until` like audit does) · effort:S.
- [ ] **SSE write-timeout** — `F-SV-004` (global `WriteTimeout: 60s` severs SSE) · effort:S — exempt streaming routes. _(Verify whether the streaming framework already resets the deadline before acting.)_
- [ ] **Minor robustness** — `F-VL-002` (defer-close before read), `F-VL-003` (empty JSON wrapper), `F-MW-004/005` (statusWriter interface forwarding + double-WriteHeader guard), `F-LW-003` (`store==nil`→success contract), `F-LH-005/F-LH-007/F-LH-008` · effort:S.

## Workstream clusters (batch these)

- **Tenant-isolation hardening (highest value):** `X-SEC-001` (P0) + `X-SEC-002` + `F-HD-002` — all about scoping a request to the caller's tenant; design the ownership-check pattern once and apply everywhere.
- **Input-limits & error-envelope:** `X-DOS-001` + `X-LIMIT-001` + `X-ERR-001` + `F-TN-006` — one middleware pass for body caps + shared `parseLimit` + shared `writeError`.
- **Lifecycle & concurrency:** `F-LW-001` + `X-SHUT-001` + `F-SV-001` + `F-WT-001/003` — the async-logging and watcher teardown paths.
- **Route-policy unification:** `X-AUTHZ-001` + `F-HD-001` + `F-CO-005` + `F-PL-001` — one role-floor table.

## Needs investigation (low-confidence / dependency-gated)

- [ ] `X-DOS-001` YAML half — confirm `config.ValidateBytes`'s YAML lib bounds alias expansion (billion-laughs); if not, the body cap alone is insufficient. Check `internal/config`.
- [ ] `X-SEC-001` store half — decide whether tenant scoping lives in `tenancy/store.go` signatures or the handler; the store package was not reviewed here.
- [ ] `F-LH-003` SSE cross-tenant interference — confirm whether the broadcaster should grow a per-tenant subscription predicate (touches `log_broadcaster.go`).
